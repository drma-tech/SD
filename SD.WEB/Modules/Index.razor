@page "/"
@page "/{culture}"

@using SD.WEB.Modules.Collections.Core
@using SD.WEB.Modules.EditorsChoice
@using SD.WEB.Modules.FreeToWatch
@using SD.WEB.Modules.Popular
@using SD.WEB.Modules.Profile.Core
@using SD.WEB.Modules.AwardsOfTheYear.Components
@using SD.WEB.Modules.Trailers.Components
@using SD.WEB.Modules.News.Components
@inherits PageCore<Index>

@inject WatchingListApi WatchingApi
@inject WishListApi WishApi
@inject TmdbListApi TmdbListApi
@inject TmdbSearchKeywordApi TmdbSearchKeywordApi

<HeadComponent Title="@Translations.Title.AppSubtitle" Description="@Translations.Description.AppDescription" Url=@($"/{Culture}") Index="@Culture.NotEmpty()" Shared="true"></HeadComponent>

<PageSectionComponent IsPageHeader="true" Image="icon/icon-71.png" Title="@AppInfo.Title" Subtitle="@Translations.Title.AppSubtitle"></PageSectionComponent>

<PageSectionComponent Title="@Translations.Module.Landing.Search">
    <BodyFragment>
        <MudTabs MinimumTabWidth="100" TabPanelsClass="@Css.Build().Small("pt")" @bind-ActivePanelIndex="@AppStateStatic.Index">
            <ChildContent>
                <MudTabPanel Text="@Translations.Module.Landing.Title">
                    <MudTextField T="string" Label="@Translations.Module.Landing.EnterTitle" Placeholder="Example: Superman, Dune, Dexter, Andor, Henry Cavill, Leonardo DiCaprio" @bind-Text="@AppStateStatic.Query" Immediate="true" OnKeyDown="@KeyPress"
                                  AdornmentIcon="@Icons.Material.Filled.Search" Adornment="@(AppStateStatic.Query.NotEmpty() ? Adornment.End : Adornment.None)" AdornmentColor="Color.Primary" Variant="Variant.Outlined"
                                  OnAdornmentClick="@(() =>{ if (AppStateStatic.Query.NotEmpty()) Navigation.NavigateTo($"/{Culture}/search"); })" Clearable="true"
                                  HelperText="@Translations.Module.Landing.TypeSomething" InputType="InputType.Text">
                    </MudTextField>
                </MudTabPanel>
                <MudTabPanel Text="@Translations.Module.Landing.Keywords">
                    <MudTextField T="string" Label="@Translations.Module.Landing.EnterKeyword" Placeholder="Example: love, robot, lost, animation, superhero, family" @bind-Text="@AppStateStatic.Query" Immediate="true"
                                  AdornmentIcon="@Icons.Material.Filled.Search" Adornment="@(AppStateStatic.Query.NotEmpty() ? Adornment.End : Adornment.None)" AdornmentColor="Color.Primary" Variant="Variant.Outlined"
                                  OnKeyDown="@KeyPressKeyboard" OnAdornmentClick="@(async() => { if (AppStateStatic.Query.NotEmpty()) await SearchKeyWords(); })" Clearable="true" 
                                  OnClearButtonClick="@(() => AppStateStatic.Keywords = [])" HelperText="@Translations.Module.Landing.SearchKeywords" InputType="InputType.Text">
                    </MudTextField>
                    @foreach (var item in AppStateStatic.Keywords)
                    {
                        <MudChip T="string" Color="Color.Info" Size="AppStateStatic.Size" OnClick="@(() => Navigation.NavigateTo($"/{Culture}/search?keyword={item.id}"))">
                            @item.name
                        </MudChip>
                    }
                    @if (AppStateStatic.Keywords.NotEmpty())
                    {
                        <MudChip T="string" Color="Color.Secondary" Size="AppStateStatic.Size" OnClick="@LoadMore" Disabled="@DisableLoadMore">
                            @Translations.Button.LoadMore
                        </MudChip>
                    }
                </MudTabPanel>
                <MudTabPanel Text="@Translations.Module.Landing.Advanced">
                    <MudToolBar Gutters="false" WrapContent="true">
                        <MudSelect T="MediaType?" @bind-Value="AppStateStatic.Type" Variant="Variant.Outlined" Label="@Translations.Module.Landing.Type" Clearable="true" Class="me-2">
                            @foreach (var item in Types)
                            {
                                <MudSelectItem Value="(MediaType?)item.Value">@item.Name</MudSelectItem>
                            }
                        </MudSelect>
                        @if (AppStateStatic.Type == MediaType.movie)
                        {
                            <MudSelect T="MovieGenre?" @bind-Value="AppStateStatic.MovieGenre" Variant="Variant.Outlined" Label="@Translations.Module.Landing.Genre" Clearable="true" Disabled="AppStateStatic.Type == null" Class="me-2">
                                @foreach (var item in MovieGenres)
                                {
                                    <MudSelectItem Value="(MovieGenre?)item.Value">@item.Name</MudSelectItem>
                                }
                            </MudSelect>
                        }
                        else
                        {
                            <MudSelect T="TvGenre?" @bind-Value="AppStateStatic.TvGenre" Variant="Variant.Outlined" Label="@Translations.Module.Landing.Genre" Clearable="true" Disabled="AppStateStatic.Type == null" Class="me-2">
                                @foreach (var item in TvGenres)
                                {
                                    <MudSelectItem Value="(TvGenre?)item.Value">@item.Name</MudSelectItem>
                                }
                            </MudSelect>
                        }
                        <MudSelect T="string" @bind-Value="AppStateStatic.SortBy" Variant="Variant.Outlined" Label="@Translations.Module.Landing.SortBy" Class="me-2">
                            <MudSelectItem Value="@("original_title.asc")">original_title.asc</MudSelectItem>
                            <MudSelectItem Value="@("original_title.desc")">original_title.desc</MudSelectItem>
                            <MudSelectItem Value="@("popularity.asc")">popularity.asc</MudSelectItem>
                            <MudSelectItem Value="@("popularity.desc")">popularity.desc</MudSelectItem>
                            <MudSelectItem Value="@("revenue.asc")">revenue.asc</MudSelectItem>
                            <MudSelectItem Value="@("revenue.desc")">revenue.desc</MudSelectItem>
                            <MudSelectItem Value="@("primary_release_date.asc")">primary_release_date.asc</MudSelectItem>
                            <MudSelectItem Value="@("title.asc")">title.asc</MudSelectItem>
                            <MudSelectItem Value="@("title.desc")">title.desc</MudSelectItem>
                            <MudSelectItem Value="@("primary_release_date.desc")">primary_release_date.desc</MudSelectItem>
                            <MudSelectItem Value="@("vote_average.asc")">vote_average.asc</MudSelectItem>
                            <MudSelectItem Value="@("vote_average.desc")">vote_average.desc</MudSelectItem>
                            <MudSelectItem Value="@("vote_count.asc")">vote_count.asc</MudSelectItem>
                            <MudSelectItem Value="@("vote_count.desc")">vote_count.desc</MudSelectItem>
                        </MudSelect>
                        <MudIconButton Icon="@Icons.Material.Filled.Search" Color="Color.Primary" OnClick="@(() => { Navigation.NavigateTo($"/{Culture}/search"); })"></MudIconButton>
                    </MudToolBar>
                </MudTabPanel>
            </ChildContent>
        </MudTabs>
    </BodyFragment>
</PageSectionComponent>

<PopularIndexComponent WatchingList="Watching" WishList="Wish" Culture="@Culture"></PopularIndexComponent>

<FreeToWatchComponent WatchingList="Watching" WishList="Wish" Culture="@Culture"></FreeToWatchComponent>

<Advertising Slot="@AdSlot.Middle"></Advertising>

<EditorsChoiceComponent Watching="Watching" Wish="Wish" Culture="@Culture"></EditorsChoiceComponent>

<AwardsOfTheYearComponent Watching="Watching" Wish="Wish" Culture="@Culture"></AwardsOfTheYearComponent>

<Advertising Slot="@AdSlot.Middle"></Advertising>

<PageSectionComponent Title="@Translations.Title.ExpectedMovieOfYear.Format(DateTime.Now.Year)" Description="@Translations.Description.ExpectedMovies.Format(2026)">
    <BodyFragment>
        <MudTabs KeepPanelsAlive="true" MinimumTabWidth="100" ApplyEffectsToContainer="true" TabPanelsClass="@Css.Build().Small("pt")">
            <Header>
                <MudTooltip Text="Expand">
                    <MudIconButton Icon="@Icons.Material.Filled.Fullscreen" Color="Color.Primary" OnClick="@(() => Navigation.NavigateTo($"/{Culture}/list/{(int)EnumLists.ExpectedMovieOf2026}"))" />
                </MudTooltip>
            </Header>
            <ChildContent>
                <MudTabPanel Text="All">
                    <MediaListComponent MediaListApi="TmdbListApi" List="@EnumLists.ExpectedMovieOf2026" TitleHead="@Translations.Title.ExpectedMovieOfYear.Format(DateTime.Now.Year)" Watching="Watching" Wish="Wish" Culture="@Culture" />
                </MudTabPanel>
            </ChildContent>
        </MudTabs>
    </BodyFragment>
</PageSectionComponent>

<MudGrid Spacing="@Css.SpaceSmall">
    <MudItem xs="12" sm="6">
        <TrailersComponent Culture="@Culture"></TrailersComponent>
    </MudItem>
    <MudItem xs="12" sm="6">
        <NewsComponent Culture="@Culture"></NewsComponent>
    </MudItem>
</MudGrid>

@code {
    private WatchingList? Watching { get; set; }
    private WishList? Wish { get; set; }

    public List<EnumFieldObject<MediaType>> Types { get; set; } = [];
    public List<EnumFieldObject<MovieGenre>> MovieGenres { get; set; } = [];
    public List<EnumFieldObject<TvGenre>> TvGenres { get; set; } = [];

    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();

        WatchingApi.DataChanged += model => { Watching = model; StateHasChanged(); };
        WishApi.DataChanged += model => { Wish = model; StateHasChanged(); };

        Types = EnumHelper.GetList<MediaType>().Where(p => p.Value != MediaType.person).ToList();
        MovieGenres = EnumHelper.GetList<MovieGenre>();
        TvGenres = EnumHelper.GetList<TvGenre>();
    }

    protected override async Task LoadAuthenticatedDataAsync(CancellationToken token)
    {
        Watching = await WatchingApi.Get(null, token);
        Wish = await WishApi.Get(null, token);
    }

    private void KeyPress(KeyboardEventArgs args)
    {
        if (AppStateStatic.Query.Empty()) return;

        if (args.Key == "Enter")
        {
            Navigation.NavigateTo($"/{Culture}/search");
        }
    }

    private async Task KeyPressKeyboard(KeyboardEventArgs args)
    {
        if (AppStateStatic.Query.Empty()) return;

        if (args.Key == "Enter")
        {
            await SearchKeyWords();
        }
    }

    private int _currentPage = 1;
    public bool DisableLoadMore { get; set; }

    private async Task SearchKeyWords()
    {
        AppStateStatic.Keywords = [];
        _currentPage = 1;
        Dictionary<string, string> parameters = new Dictionary<string, string> { { "query", AppStateStatic.Query ?? "" }, { "sort_by", "popularity.desc" } };

        var result = await TmdbSearchKeywordApi.GetKeywords(AppStateStatic.Keywords, parameters, _currentPage++, cts.Token);
        AppStateStatic.Keywords = result.list;

        DisableLoadMore = result.lastPage || AppStateStatic.Keywords.Count >= 100;
    }

    private async Task LoadMore()
    {
        Dictionary<string, string> parameters = new Dictionary<string, string> { { "query", AppStateStatic.Query ?? "" }, { "sort_by", "popularity.desc" } };

        var result = await TmdbSearchKeywordApi.GetKeywords(AppStateStatic.Keywords, parameters, _currentPage++, cts.Token);
        AppStateStatic.Keywords = result.list;
        DisableLoadMore = result.lastPage || AppStateStatic.Keywords.Count >= 100;
    }

}
