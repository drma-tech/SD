@using SD.WEB.Modules.Profile.Components
@using SD.WEB.Modules.Profile.Core
@using SD.WEB.Shared.Core
@inherits ComponentCore<MyWatchingListPopup>

@inject WatchingListApi WatchingApi
@inject WishListApi WishApi

<MudDialog Style="width: 100%">
    <DialogContent>
        <WatchingComponent TypeParam="MediaType" ShowHeader="false" FullScreen="true" Watching="Watching" Wish="Wish"
                           ActionsMovie="Actions" ActionsTv="Actions" Culture="@Culture">
        </WatchingComponent>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="@(() => { MudDialog?.Close(); })">
            @Translations.Button.Close
        </MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] private IMudDialogInstance? MudDialog { get; set; }

    [Parameter][EditorRequired] public ComponentActions<WatchingList?> Actions { get; set; }
    [Parameter][EditorRequired] public WatchingList? Watching { get; set; }
    [Parameter][EditorRequired] public WishList? Wish { get; set; }
    [Parameter][EditorRequired] public string? Culture { get; set; }

    [Parameter] public EventCallback<WatchingList?> WatchingChanged { get; set; }
    [Parameter] public EventCallback<WishList?> WishChanged { get; set; }
    [Parameter][EditorRequired] public MediaType MediaType { get; set; }

    private string Title => MediaType == MediaType.movie ? Translations.Module.Profile.MyMovieFollowing : Translations.Module.Profile.MySeriesFollowing;
    private int Quantity => MediaType == MediaType.movie ? Watching?.Movies.Count ?? 0 : Watching?.Shows.Count ?? 0;

    protected override void OnInitialized()
    {
        WatchingApi.DataChanged += model =>
        {
            Actions.StartLoading?.Invoke(null);
            Watching = model;
            WatchingChanged.InvokeAsync(model);
            Actions.FinishLoading?.Invoke(model);
            StateHasChanged();
        };
        WishApi.DataChanged += model =>
        {
            Wish = model;
            WishChanged.InvokeAsync(model);
            StateHasChanged();
        };
    }

}